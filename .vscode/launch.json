{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run test",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/runner.go",
            "env": {
                "ENV": "development"
            }
        }
    ]
}